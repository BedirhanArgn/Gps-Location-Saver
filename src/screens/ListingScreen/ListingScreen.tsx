import React from "react";
import { SafeAreaView, Text, View } from "react-native";

const ListingScreen = () => {
  return (
    <SafeAreaView>
      <Text> LISTING</Text>
    </SafeAreaView>
  );
};

export default ListingScreen;
